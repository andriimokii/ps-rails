<%= form_with model: genre, local: true, data: { turbo: false } do |form| %>
  <%= render "shared/errors", object: genre %>

  <%= form.label :name %>
  <%= form.text_field :name, autofocus: true %>

  <%= form.submit %>
<% end %>