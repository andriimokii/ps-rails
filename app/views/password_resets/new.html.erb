<h1>Email to Reset Password</h1>

<%= form_with url: password_resets_path, local: true, 
                        data: { turbo: false } do |form| %>

    <%= form.label :email %>
    <%= form.email_field :email %>

    <%= form.submit "Reset Password" %>
<% end %>